spring:
  data:
    mongodb:
      uri: mongodb://mongo:27017/algoquest_test
  main:
    allow-bean-definition-overriding: true
  autoconfigure:
    exclude: # désactive Flapdoodle
      - org.springframework.boot.autoconfigure.mongo.embedded.EmbeddedMongoAutoConfiguration

logging:
  level:
    root: WARN
